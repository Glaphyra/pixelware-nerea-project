CREATE TABLE Users (id INTEGER IDENTITY PRIMARY KEY, password VARCHAR(20) NOT NULL, email VARCHAR(40) NOT NULL, country VARCHAR(40) NOT NULL, birthdate DATE NOT NULL);
CREATE TABLE History (id INTEGER IDENTITY PRIMARY KEY, city VARCHAR(60) NOT NULL, degrees DOUBLE NOT NULL, FK_user INTEGER, FOREIGN KEY (FK_user) REFERENCES Users(id));